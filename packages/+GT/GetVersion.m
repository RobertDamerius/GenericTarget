function v = GetVersion()
    %GT.GetVersion Get the current version of the Generic Target toolbox.
    % 
    % RETURN
    % v ... A 1-by-3 vector representing the current version of type uint32. The format is [major, minor, patch].

    v = uint32([1,0,0]);
end

